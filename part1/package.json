{
  "name": "algorithms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "ts-node-dev --respawn src/reverse-string.ts",
    "build": "del ./dist && tsc -p .",
    "watch": "tsc -w",
    "test": "jest --watchAll --no-cache",
    "lint": "tslint -c tslint.json -p tsconfig.json"
  },
  "jest": {
    "preset": "ts-jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.8",
    "del-cli": "^3.0.1",
    "jest": "^26.2.2",
    "ts-jest": "^26.1.4",
    "ts-node-dev": "^1.0.0-pre.56",
    "tslint": "^6.1.3",
    "typescript": "^3.9.7"
  }
}
